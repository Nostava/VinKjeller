{
  "name": "vinkjeller",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server",
    "enrich": "node server/scripts/enrich.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
